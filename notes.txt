gcc draw_line.c  -lmlx -framework OpenGL -framework AppKit minilibx_macos/libmlx.a read_map.c buttons.c attune_lines.c draw_map.c window_works.c fdf.c libft/libft.a get_lines.c -g

gcc draw_line.c  -lmlx -framework OpenGL -framework AppKit minilibx_macos/libmlx.a read_map.c buttons.c attune_lines.c draw_map.c window_works.c fdf.c libft/libft.a get_lines.c -g -fsanitize=address  - покажет источник наебнутой памяти
управление:
ESC (53) - выход
1 - параллельная
2 - изометрия
мышь - верчение
стрелки - перемещение карты
колесо мыши - зум
пробел блок движения
https://code.woboq.org/qt5/include/X11/X.h.html   - коды из X.h

ОПТИМИЗАЦИЯ:
-сброс координат: хранить базовые значения и запоминать движение по стрелкам и движение при зуме, модифицировать
эти значения в функциях и с ними current? пиздос, придется проверять эту поебень в зуме и двигать там тоже.
==============================================>
Так мувмент просто двигает каррент на Х или У * модификатор (сейчас 5 пикселей). Мувмент сделать чтобы запоминал позицию.
Зум делает зум оригинальных координат а потом двигает на запомненную позицию.

++++++++++
чтобы изображение не скакало, нужно двигать все координаты к центру, см тетрадь. нужно определить середину (хмакс - хмин)/2
если в левой половине - сдвиг вправо, если правая половина - сдвиг влево, соотв-но то же самое с высотой. середину двигать
ненужно
в таком случае изображение схлопнется в середину, но эту хуйню нужно будет высчитывать еще, блядство, с флоатом скорее всего,
иначе середина будет скакать!!!! нет можно же без флоата просто на два умножить кайф

==========================================
Во-первых нужно в зуме и движении сохранять модификацию высоты,
Во-вторых нужно как-то обойти условия на z0 и z1 != 0 потому что эта параша приравнявшись к 0 ломается. Надо короч
скопировать структуру line с оригинальными координатами и в копии уже все делать, срвнивая с оригиналом


====
Дроу лайн сделать таким, чтобы сначала он смотрел в градиент тайп, а потом вызывал соответствующую рисовалку

=======
перенести swap_em в либу, обозвать ft_swap